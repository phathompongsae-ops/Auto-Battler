# Current Project Handoff

Generated by Claude Code (Sonnet) so that Coco (ChatGPT) or any other agent can resume this
repository from scratch — with no chat history — if Claude Code's quota runs out mid-task.

Last updated: 2026-07-17T16:22Z, from branch `fix/hero-drag-drop-and-remove-novice`.

All facts below were read directly from the repository (`git log`, `git status`, `git ls-tree`,
GitHub PR API) at the time of writing — nothing here is guessed or assumed.

---

## Current Repository State

- **Branch (this handoff was written on):** `fix/hero-drag-drop-and-remove-novice`
- **HEAD commit:** `6da1e9cf0f72dcd1bc244774641ad6173e7ae15d` — "Fix Android touch drag & drop for
  hero placement, add real-time tile highlight"
- **Working tree:** clean (no uncommitted changes before this handoff commit)
- **`main` HEAD:** `324f65ffde5737a5d462a4d678a9161724797c7b` — "Improve mobile side panel layout"
- **Current PR for this branch:** [#10](https://github.com/phathompongsae-ops/Auto-Battler/pull/10)
  — open, not draft, not merged, `mergeable_state: clean`. **Do not merge** — for review only.

---

## Completed Work (merged into `main`)

In rough chronological order, all confirmed as ancestors of `main`:

- 3D board/scene, hero systems (Shop, Bench, Evolution, Synergy, Star system), combat engine,
  GitHub Pages deploy — merged via **PR #2** (`claude/threejs-2-5d-clean-v5-e80mbk`).
- `5c581bb` — Fix melee units pathing to occupied targets.
- `6a0c184` — Improve mobile topbar layout (from branch `mobile-topbar-responsive-1`).
- `37044b6` — Make field capacity follow player level (from branch
  `field-capacity-by-level-v2`; an earlier `field-capacity-by-level` v1 branch was superseded
  and is **not** merged — safe to ignore/delete).
- `324f65f` — Improve mobile side panel layout (from branch `mobile-side-panels-responsive-2`).
  **This is current `main` HEAD.**
- `6da1e9c` (this branch, PR #10, **not yet merged**) — Android touch drag & drop fix +
  real-time placement highlight. See PR Summary below.

`agent/combat-targeting-fix` (**PR #1**) was closed **without merging** — its commits are not
in `main`. Do not assume its targeting/taunt/warp/projectile work is live.

---

## Open PR Summary

All open PRs target `main` at `324f65f`. None have been merged. None should be merged without
the user's explicit go-ahead.

### PR #10 — `fix/hero-drag-drop-and-remove-novice` (this branch — Claude Code)
- **Purpose:** Fix Android touch drag & drop (root cause: canvas missing `touch-action`,
  letting native scroll-gesture recognition fire `pointercancel` mid-drag), add real-time
  single-tile placement-preview highlight, verify Novice is absent from the runtime.
- **Files touched:** `autochess.html` (1-line CSS), `src/game.js` (drag/highlight logic).
- **Merge-ready:** Yes, mechanically (`mergeable_state: clean`). Held back only because the
  task instructions say not to merge.
- **Blocker:** None technical. **Will textually conflict with PR #9** if both are merged,
  since both touch the same `<style>`/canvas region of `autochess.html` — merge one, then
  rebase the other, don't merge both blind.

### PR #9 — `fix/landscape-only-mobile-ui` (Claude Code)
- **Purpose:** Pure-CSS landscape-only lock (portrait overlay via `@media (orientation:
  portrait)`, zero JS) + closes out the Shop-Drawer white-rectangle investigation
  (`docs/SHOP_DRAWER_ANDROID_VERIFICATION.md` — confirmed non-reproducible on real Android
  hardware, classified as a sandbox/SwiftShader compositor artifact).
- **Files touched:** `autochess.html`, `docs/SHOP_DRAWER_ANDROID_VERIFICATION.md` (new).
- **Merge-ready:** Yes (`mergeable_state: clean`).
- **Blocker:** Same PR #10 conflict risk noted above.

### PR #8 — `coco/demo1-asset-audit` (Coco)
- **Purpose:** Audits Map 1 / Demo 1 asset coverage; adds a canonical logical-ID → file-path
  mapping for the 7 Class 1 heroes + Map 1 enemies/bosses; adds `data/demo1-assets.json`
  (proposed lock) and a dependency-free validator.
- **Files touched:** `docs/DEMO1_ASSET_AUDIT.md`, `docs/DEMO1_CANONICAL_ASSET_MAP.md`,
  `data/demo1-assets.json`, `tools/validate-demo1-assets.mjs`. No runtime files touched.
- **Merge-ready:** Yes mechanically (`mergeable_state: clean`), doc/data-only.
- **Blocker:** Its own findings flag Slime/Orc assets as unresolved, and Golem
  naming/Champion-asset duplication as needing "a later controlled runtime/asset decision" —
  i.e. it documents a **design-vs-runtime mismatch** (see Runtime Status below) rather than
  fixing it. Data-only; safe to merge for reference but doesn't change runtime behavior.

### PR #7 — `codex/game-data-contract-v1` (Codex) — **Draft**
- **Purpose:** Documentation-first "Game Data Contract v1" + `DEMO1_DATA_POLICY.md` (the
  canonical Demo 1 rules quoted below), JSON schema, and validator scripts. No runtime
  migration yet.
- **Files touched:** multiple new files under `docs/`, `data/`, `tools/` — no `src/game.js`,
  no `autochess.html`.
- **Merge-ready:** No — explicitly kept as Draft "until the contract and Demo 1 policy are
  reviewed against the current runtime after CC finishes the active Drag & Drop /
  Novice-removal task" (i.e. after this PR #10).
- **Blocker:** Needs a reconciliation pass against actual runtime values (bench=8 vs
  design=5, Stage 10 boss = Bone Dragon vs design = Golem) before it can be treated as final.

### PR #6 — `codex/effect-projectile-frameworks` (Codex) — **Draft**
- **Purpose:** Standalone pooled VFX/projectile presentation framework, **not wired into the
  game**. Explicitly does not touch targeting/damage/HP/game loop.
- **Merge-ready:** No — draft, kept until this PR #10 lands and integration is reviewed.

### PR #5 — `codex/animation-framework` (Codex) — **Draft**
- **Purpose:** Standalone sprite animation state-machine framework, **not wired into the
  game**. Explicitly does not touch combat/movement/camera.
- **Merge-ready:** No — same reasoning as PR #6.

### PR #4 — `codex/asset-manager-framework` (Codex) — **Draft**
- **Purpose:** Standalone asset preload/cache/ref-count manager, **not wired into the game**.
- **Merge-ready:** No — same reasoning as PR #6.

### PR #3 — `codex/project-handoff-docs-clean` (Codex) — **Draft**
- **Purpose:** An earlier documentation-only handoff/audit package (agent ownership rules,
  regression checklist, melee-movement audit, asset-inventory audit). Predates this file;
  overlaps in intent but not in filename (`docs/CURRENT_PROJECT_HANDOFF.md` did not exist
  before this commit — verified via `git log --all` for that path returning nothing).
- **Merge-ready:** No — draft.
- **Note:** This file (`docs/CURRENT_PROJECT_HANDOFF.md`) is the new single canonical handoff
  going forward per the current task's explicit instructions. PR #3's docs are a separate,
  older artifact — not superseded automatically, just not duplicated here.

---

## Runtime Status

Everything below reflects the actual code on `main` + PR #10 (this branch), read directly
from `src/game.js`. Differences from the canonical design docs (PR #7/#8) are called out
explicitly, not hidden.

| System | Status |
|---|---|
| **Drag & Drop** (Bench→Board, Board→Board, Board→Bench) | Working. Verified via Playwright with real mouse events and genuine touch events (CDP `Input.dispatchTouchEvent`) at 800×360 and 844×390. |
| **Placement Preview** | Working. Real-time single-tile hover highlight (green = legal empty player-zone tile, red = occupied), clears on drop/pointercancel/pointer-leaves-board. |
| **Android Touch** | Working as of PR #10. Root cause was missing `touch-action: none` on the canvas; fixed with a 1-line CSS change. |
| **Pointer Events** | Single unified `pointerdown`/`pointermove`/`pointerup`/`pointercancel` set drives all hero drag; no duplicate listeners added (verified via 20x repeated-drag stress test). |
| **Combat x4** | Working. Verified via Playwright: 5 heroes placed by real drag & drop, `speedMul = 4`, wave completes, no NaN, no stuck state, no new console errors. |
| **Novice Removal** | Already fully absent from `HERO_DEFS` and the live shop pool. Confirmed via a 50x `pickShopOffers()` simulation — 0 Novice occurrences; pool is exactly `fighter, swordman, archer, mage, summoner, acolyte, merchant`. Novice only exists in the disconnected `autochess-tutorial.html` legacy prototype (not part of the deployed Pages build, not linked from the reachable dev menu), `tools/gen_sprites.py`, and the asset license manifest — left untouched (no runtime reference exists to remove). |
| **Bench Capacity — Runtime vs Canonical** | **Runtime = 8** (`const MAX_BENCH = GRID_COLS;` in `src/game.js`, `GRID_COLS = 8`). **Canonical design (docs/DEMO1_DATA_POLICY.md on PR #7) = 5.** This mismatch is real and unresolved — flagged in PR #10's own description, not fixed there because changing it is a balance/scope decision, not a bug fix. |
| **Stage 10 Boss identity — Runtime vs Canonical** | **Runtime: Stage 10's boss unit is "Bone Dragon"** (`unitType:'boss'`, `bossWave()` in `src/game.js` line ~1281), with a "Golem" only appearing as an accompanying minion. **Canonical design (docs/DEMO1_DATA_POLICY.md) says Stage 10 miniboss must be `golem`**, and Bone Dragon should be a Stage 13 *featured enemy*, not the Stage 15 or Stage 10 boss. Stage 5 (`Arena Warden` → `warden`) and Stage 15 (`Immortal Champion` → `champion`) already match canonical. **This is an unresolved runtime/design mismatch, not yet fixed anywhere.** |
| **Field capacity (units placeable at once)** | `MAX_FIELD = 5`, but effective cap is `fieldCapacity() = min(level, MAX_FIELD)` — i.e. capacity scales with player level, capping at 5. This matches the canonical "5 heroes on field" expectation seen throughout design docs. |
| **Elite enemies / secret Ninja class** | Not implemented in runtime at all (no `elite` or `ninja` references in `src/game.js`). These exist only as canonical design intent in PR #7's docs — nothing to reconcile yet, just not built. |

---

## Demo 1 Canonical Rules

(Source: `docs/DEMO1_DATA_POLICY.md` on PR #7 branch `codex/game-data-contract-v1` — not yet
merged, but this is the agreed design lock referenced by the current task.)

- **Map:** 1 (`arena_ruins`, single map for Demo 1)
- **Stages:** 1–15 (matches runtime `WAVE_TOTAL = 15`)
- **Bosses:** Stage 5 = Warden (miniboss) · Stage 10 = **Golem** (miniboss) · Stage 15 =
  Immortal Champion (boss) — ⚠️ **runtime Stage 10 boss is currently Bone Dragon, not Golem**
  (see Runtime Status above)
- **Bone Dragon:** allowed only as a Stage 13 featured enemy, must never replace the Stage 15
  Champion — ⚠️ **runtime currently uses Bone Dragon as the Stage 10 boss instead**
- **Bench capacity (design):** exactly 5 — ⚠️ **runtime is currently 8**
- **Class 1 roster:** exactly 7 — `fighter, swordman, archer, mage, summoner, acolyte,
  merchant` — **runtime already matches this exactly**
- **Novice:** prohibited everywhere (hero ID `novice`, `classTier: 0`, shop entries, roster,
  fusion, spawn, fallback, localization, asset mappings) — **runtime already complies**
- **Secret class:** `ninja` unlocks as a Class Tier 3 progression reward after Stage 15; must
  never appear in the normal Shop pool — **not yet implemented in runtime, nothing to violate**
- **Elite enemies:** start at Stage 4, max 1 per stage, never on Stage 5/10/15 — **not yet
  implemented in runtime**

---

## Safe For Coco

These can be picked up immediately without touching runtime/game-loop code:

- **Data / JSON** — `data/demo1-assets.json`, `data/design/*.json` (on PR #7/#8 branches),
  any new data-only files.
- **Validator scripts** — `tools/validate-demo1-assets.mjs`, `tools/validate-game-data.mjs`,
  `tools/validate-demo1-policy.mjs` (currently only exist on unmerged branches — see Commands
  section for exact locations/usage).
- **Localization** — `data/demo1-localization.json`, `data/localization/*.json` (PR #7 branch).
- **Documentation** — anything under `docs/`, including reconciling PR #7's canonical rules
  against the Runtime Status mismatches documented above (Bench 8 vs 5, Stage 10 Bone Dragon
  vs Golem) once the user decides which side wins.
- **Asset mapping/auditing** — extending PR #8's `docs/DEMO1_CANONICAL_ASSET_MAP.md`, resolving
  the still-unresolved Slime/Orc asset gaps it flags.
- **UI (non-runtime)** — anything that is pure CSS/markup and does not touch
  `src/game.js`'s JS logic, pointer events, or the Three.js scene (PR #9's landscape overlay is
  the working example of this pattern: 100% CSS, zero JS, zero runtime risk).

## Do NOT Touch

Reserved for Claude Code (or an agent explicitly authorized for runtime work):

- `src/game.js` — combat, AI/targeting, pathfinding, drag & drop, pointer events, shop
  economy, the entire game loop.
- `autochess.html`'s `<script>`/canvas wiring (its CSS-only sections, like PR #9's overlay,
  are the one proven-safe exception).
- Three.js scene/camera/renderer setup.
- Anything that would require a Combat x4 playthrough to verify (per the task's own testing
  rule: **if you touch runtime, you must re-test Combat x4** before considering it done).

---

## Priority Queue

**P1 — Reconcile Stage 10 boss identity (Bone Dragon vs Golem) and Bench capacity (8 vs 5).**
These are the two concrete, already-documented Runtime-vs-Canonical mismatches. Nothing else
in this handoff is actionable as a "fix" until the user decides: change runtime to match the
design docs, or change the design docs to match runtime. This is a **decision**, not a code
task — see Decisions Waiting below. Once decided, it's a small, contained runtime change
(if runtime moves) that **must** be re-verified with a Combat x4 playthrough.

**P2 — Merge coordination between PR #9 and PR #10.**
Both are mechanically mergeable today but touch the same region of `autochess.html`. Whoever
merges first should trigger a rebase of the other before it's touched again. This is a
housekeeping/sequencing task, not new work — flagging it now so nobody merges both blind and
gets a silent conflict.

**P3 — Reconcile PR #7's Game Data Contract against the actual runtime constants.**
PR #7 is deliberately held as Draft pending this. Once P1 is resolved, PR #7's Demo 1 policy
doc and JSON schema should be updated (or the runtime should be) so they say the same thing.
Safe for Coco — documentation/data work.

**P4 — Resolve PR #8's flagged asset gaps (Slime, Orc, Golem-naming duplication).**
Lower priority since these are audit findings about missing/ambiguous provenance, not
blocking bugs — the game runs fine today with the existing procedural assets. Safe for Coco.

**P5 — Decide on integrating the three standalone Codex frameworks (PR #4/#5/#6).**
All three are intentionally unwired "someday" frameworks (asset manager, animation, VFX/
projectiles) with no runtime risk as long as they stay unmerged/unwired. Lowest priority
because integrating any of them is exactly the kind of "touches the game loop, needs Combat
x4 re-verification" work this handoff says to route to Claude Code, and none of them are
blocking anything else right now.

---

## Commands

There is **no `package.json`** and **no npm scripts** in this repository — verified by
`ls package.json` returning "No such file or directory" on `main` and this branch. Do not
assume `npm test`/`npm run` commands exist; none do today.

**Validators** (only exist on unmerged branches, not on `main` or this branch — fetch/checkout
the relevant branch first, e.g. `git show origin/coco/demo1-asset-audit:tools/validate-demo1-assets.mjs > /tmp/v.mjs && node /tmp/v.mjs`, or check out that branch directly):

```bash
# On branch coco/demo1-asset-audit (PR #8):
node tools/validate-demo1-assets.mjs

# On branch codex/game-data-contract-v1 (PR #7):
node tools/validate-game-data.mjs path/to/game-data.json
node tools/validate-demo1-policy.mjs path/to/game-data.json
```

**Syntax check** (always available, works on any branch):
```bash
node --check src/game.js
```

**Diff whitespace check** (required before any commit per project convention):
```bash
git diff --check
```

**Local playtest** (no committed test harness — every prior Playwright test in this project's
history was an ad-hoc scratch script run directly with `node`, not a committed suite):
```bash
python3 -m http.server 8000   # serve the repo root, then open autochess.html in a browser
```
Real Playwright verification (mouse + genuine touch via CDP `Input.dispatchTouchEvent`,
Combat x4 playthroughs, etc.) was done with throwaway scripts outside the repo — there is
nothing checked in to run "the test suite" as a single command. If Coco needs to re-verify
runtime behavior, a new ad-hoc script following the same pattern is the only option, OR route
that work back to Claude Code per the Do NOT Touch section.

**Deploy** (automatic on push to `main`, see `.github/workflows/pages.yml`):
```bash
cp autochess.html _site/index.html && cp -r assets _site/assets && cp -r src _site/src
```

---

## Known Issues

- **Bench capacity mismatch** (runtime 8 vs design 5) — see Runtime Status / P1.
- **Stage 10 boss identity mismatch** (runtime Bone Dragon vs design Golem) — see Runtime
  Status / P1.
- **PR #9 / PR #10 merge collision risk** — both touch the same `autochess.html` canvas
  region; merging both without a rebase in between will likely conflict — see P2.
- **`autochess-tutorial.html` still contains a standalone Novice definition** — confirmed
  unreachable from the deployed build and repo-root dev menu, so not a live bug, but it exists
  as dead legacy code if anyone later wires it back in.
- **No committed test suite** — all verification in this project's history has been ad-hoc
  Playwright scripts run outside the repo. There is no regression safety net beyond manual
  re-testing; anyone changing runtime code should expect to write their own throwaway
  verification script (see Commands section).
- **PR #3, #4, #5, #6, #7 are all Draft** and intentionally unintegrated — none of them
  represent a "half-finished, currently broken" state; they're deliberately standalone until
  someone does an integration pass.

---

## Decisions Waiting

Only items that genuinely need the user's judgment call — not technical unknowns:

1. **Bench capacity: keep runtime at 8, or change it to the canonical 5?** Changing it is a
   real gameplay-balance decision (fewer reserve slots changes shop/reroll strategy), not a
   bug fix — needs the user's call before any agent touches `MAX_BENCH`/`GRID_COLS`.
2. **Stage 10 boss: keep Bone Dragon as the Stage 10 boss, or swap in Golem (moving Bone
   Dragon to a Stage 13 featured-enemy role as canonically specified)?** This changes a
   memorable boss fight's identity/stats — needs the user's confirmation before any agent
   edits `bossWave()`.
3. **Merge order for PR #9 vs PR #10** — cosmetic/sequencing, but the user should say which
   one they want live first, since whichever merges second will need a rebase.
4. **Whether to formally adopt PR #7's Game Data Contract / Demo 1 Policy as binding**, or
   keep runtime as the source of truth and treat those docs as aspirational until P1/P3 are
   resolved.

No other section of this document required a guess — everything else was read directly from
the repository or the GitHub API.
