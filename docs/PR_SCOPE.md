# PR Scope

This pull request must contain documentation files only under `docs/`.

It must not contain changes to:
- `src/game.js`
- `autochess.html`
- assets
- workflows
- dependencies
- runtime configuration

Reviewers should reject any unrelated file change.
